# Mirror of [project.optional-dependencies].dev in pyproject.toml.
# Prefer `pip install -e ".[dev]"` for editable installs.
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
hypothesis>=6.0
ruff>=0.5
mypy>=1.10